# DILImap 项目整体设计思路总结

## 🎯 项目核心目标

**DILImap 项目做了一件什么事？**

构建一个基于转录组学数据的**药物性肝损伤（DILI）预测系统**，能够：
- 输入：化合物的转录组数据（RNA-seq计数矩阵）
- 输出：DILI风险概率和安全边际（Safety Margin）

**核心价值**：在药物研发早期阶段，通过体外实验的转录组数据预测化合物的肝毒性，避免昂贵的临床试验失败。

---

## 🏗️ 整体设计架构

### 设计理念：多层次特征提取 + 集成学习

```
原始转录组数据
    ↓
【第一层：基因级特征】
    差异表达分析（DESeq2）
    → 每个基因的log₂折叠变化和p值
    ↓
【第二层：通路级特征】
    通路富集分析（WikiPathways）
    → 每个通路的激活分数（-log₁₀ p值）
    ↓
【第三层：模型级预测】
    机器学习模型（随机森林集成）
    → DILI风险概率 + 安全边际
```

---

## 📊 完整数据流程

### 阶段一：数据准备（1.1-1.3）

**目标**：整合多源数据，建立标准化的训练数据集

#### 1.1 DILI标签整合
- **输入**：多个临床数据库（DILIrank、LiverTox、DILIst）
- **处理**：
  - 化合物名称标准化
  - 多源标签整合（建立共识标签）
  - 提取DILI机制信息
- **输出**：标准化的DILI标签表
- **设计思路**：通过多源数据交叉验证，提高标签可靠性

#### 1.2 Cmax值收集
- **输入**：20+研究中的Cmax数据
- **处理**：
  - 整合多个来源的Cmax值
  - 计算中位数Cmax（共识值）
  - 区分总Cmax和游离Cmax
- **输出**：每个化合物的Cmax值
- **设计思路**：Cmax用于剂量标准化和安全边际计算

#### 1.3 细胞活力实验数据处理
- **输入**：原始细胞活力实验数据
- **处理**：
  - 剂量-响应曲线拟合
  - 计算IC10值（10%抑制浓度）
  - 手动补充缺失数据
- **输出**：每个化合物的IC10值
- **设计思路**：IC10用于安全边际计算（IC10/Cmax）

---

### 阶段二：模型训练（2.1-2.3）

**目标**：从转录组数据中提取特征并训练预测模型

#### 2.1 基因特征提取
- **输入**：原始RNA-seq计数矩阵（训练数据）
- **处理流程**：
  1. **质量控制（QC）**：
     - LDH检测（细胞死亡标志）
     - 总RNA计数过滤（去除低质量细胞）
     - 线粒体RNA百分比（<9%）
     - 跨重复相关性检查
     - 肝细胞保真度验证（检查标志基因表达）
  
  2. **差异表达分析（DESeq2）**：
     - 对每个化合物-剂量组合，计算相对于DMSO对照的差异表达
     - 输出：log₂折叠变化、调整后的p值（FDR）
  
- **输出**：基因级特征矩阵（基因 × 化合物-剂量）
- **设计思路**：识别受化合物影响的基因，作为第一层特征

#### 2.2 通路特征提取
- **输入**：DESeq2结果（基因的FDR值）
- **处理流程**：
  1. **通路富集分析（WikiPathways）**：
     - 使用gseapy进行富集分析
     - 计算每个通路的激活分数（-log₁₀ p值）
     - 量化通路激活程度
  
- **输出**：通路级特征矩阵（通路 × 化合物-剂量）
- **设计思路**：将基因级信息聚合到通路级，减少维度，提高生物学可解释性

#### 2.3 ToxPredictor模型训练
- **输入**：通路特征 + DILI标签 + Cmax值
- **处理流程**：
  1. **数据准备**：
     - 映射DILI标签和Cmax值
     - 定义剂量特异性DILI标签（考虑剂量/Cmax比值）
     - 过滤不活跃化合物
     - 特征过滤（只保留显著通路）
  
  2. **模型选择与超参数优化**：
     - 测试8种机器学习模型：
       * 逻辑回归（Logistic Regression）
       * 支持向量机（SVC）
       * 随机森林（Random Forest）
       * 梯度提升（Gradient Boosting）
       * XGBoost
       * LightGBM
       * 多层感知器（MLP）
       * 直方图梯度提升（HistGradientBoosting）
     - 网格搜索超参数
     - 分组交叉验证（确保同一化合物不会同时出现在训练集和验证集）
  
  3. **模型评估指标**：
     - 标准指标：AUC、精确率、召回率、F1分数
     - 特殊指标：
       * 跨折相关性（模型稳健性）
       * 剂量单调性（预测应随剂量增加而增加）
  
  4. **最终模型选择**：
     - 选择随机森林作为最终模型
     - 集成多个交叉验证折的模型
  
- **输出**：训练好的ToxPredictor模型（随机森林集成）
- **设计思路**：
  - 多模型对比确保选择最优模型
  - 分组交叉验证避免数据泄露
  - 特殊评估指标确保模型符合生物学预期

---

### 阶段三：模型验证（3.1-3.3）

**目标**：在独立验证集上评估模型性能

#### 3.1-3.3 验证流程
- **输入**：独立的验证数据（未见过的化合物）
- **处理**：重复训练阶段的流程（QC → DESeq2 → 通路富集 → 预测）
- **评估**：
  - 预测DILI风险概率
  - 计算安全边际（MOS = IC10/Cmax）
  - 性能指标：敏感性、特异性、平衡准确率
- **设计思路**：独立验证集确保模型泛化能力

---

### 阶段四：结果分析与基准测试（4.1-4.3）

**目标**：生成论文图表并与现有方法对比

#### 4.1 主要结果图表
- 合并交叉验证和验证结果
- 生成论文中的主要图表（图2-5）
- 展示模型性能和安全边际分布

#### 4.2 与计算机模型对比
- **对比模型**：
  - DILIGeNN（图神经网络）
  - DILIPredictor（随机森林 + 分子描述符）
  - TxGemma（Google DeepMind的大语言模型，2B/9B/27B变体）
- **设计思路**：公平对比，确保测试集化合物在训练时未见

#### 4.3 与体外模型对比
- **对比模型**：20+行业标准的体外实验模型
- **评估指标**：敏感性、特异性、平衡准确率
- **设计思路**：证明转录组学方法可以媲美或超越传统体外实验

---

## 🧠 核心设计思路

### 1. **多层次特征提取**
```
基因级 → 通路级 → 模型级
```
- **为什么**：从细粒度到粗粒度，逐步抽象，提高可解释性
- **优势**：通路级特征比基因级更稳定，生物学意义更明确

### 2. **剂量感知的标签系统**
- **关键创新**：不是简单的有/无DILI，而是考虑剂量与Cmax的关系
- **规则**：
  - 低剂量 + 无DILI标签 → 无DILI
  - 高剂量 + 有DILI标签 → 有DILI
  - 其他情况 → 不确定（不用于训练）
- **设计思路**：更符合实际应用场景（需要考虑给药剂量）

### 3. **严格的数据分割策略**
- **分组交叉验证**：按化合物分组，确保同一化合物的所有剂量不会同时出现在训练集和验证集
- **独立验证集**：完全独立的化合物集合
- **设计思路**：避免数据泄露，确保模型泛化能力

### 4. **多维度模型评估**
- **标准指标**：AUC、精确率、召回率等
- **特殊指标**：
  - 跨折相关性：评估模型稳健性
  - 剂量单调性：确保预测符合生物学预期
- **设计思路**：不仅关注预测准确率，还关注模型的生物学合理性

### 5. **安全边际计算**
- **公式**：MOS = IC10 / Cmax
- **阈值**：MOS < 80 表示有DILI风险
- **设计思路**：提供可解释的安全评估，而不仅仅是概率

---

## 🔄 完整工作流程

```
┌─────────────────────────────────────────────────────────────┐
│                    阶段一：数据准备                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 1.1 DILI标签 │  │ 1.2 Cmax值   │  │ 1.3 IC10值   │      │
│  │   整合       │  │   收集       │  │   计算       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│           │                │                │                │
│           └────────────────┴────────────────┘                │
│                            │                                 │
│                    【标准化数据集】                            │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    阶段二：模型训练                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 2.1 基因特征提取                                      │   │
│  │   原始计数矩阵 → QC → DESeq2 → 基因特征矩阵          │   │
│  └──────────────────────────────────────────────────────┘   │
│                             │                                │
│                             ▼                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 2.2 通路特征提取                                      │   │
│  │   基因特征 → WikiPathways富集 → 通路特征矩阵         │   │
│  └──────────────────────────────────────────────────────┘   │
│                             │                                │
│                             ▼                                │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 2.3 ToxPredictor训练                                  │   │
│  │   通路特征 + DILI标签 → 模型选择 → 最终模型          │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    阶段三：模型验证                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ 独立验证集 → 重复2.1-2.3流程 → 性能评估              │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                    阶段四：结果分析                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ 4.1 主要图表 │  │ 4.2 计算模型 │  │ 4.3 体外模型 │      │
│  │   生成       │  │   对比       │  │   对比       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 💡 关键设计决策

### 1. **为什么选择通路级特征而不是基因级？**
- **维度问题**：基因数量（~20,000）远大于样本数，容易过拟合
- **生物学意义**：通路级特征更稳定，生物学解释性更强
- **实际效果**：通路级特征在验证集上表现更好

### 2. **为什么使用随机森林而不是深度学习？**
- **数据规模**：训练样本数有限（~250个化合物）
- **可解释性**：随机森林可以提供特征重要性
- **稳健性**：在小数据集上表现更稳定
- **实际对比**：在基准测试中，随机森林表现与深度学习模型相当

### 3. **为什么需要剂量特异性标签？**
- **现实场景**：同一化合物在不同剂量下可能有不同的DILI风险
- **Cmax归一化**：考虑最大血药浓度，使不同化合物可比较
- **更精确**：比简单的有/无DILI标签更符合实际应用

### 4. **为什么需要安全边际？**
- **可解释性**：MOS = IC10/Cmax 有明确的生物学意义
- **决策支持**：提供具体的风险评估阈值（MOS < 80）
- **临床应用**：比单纯的概率更容易被药物研发人员理解

---

## 🎓 项目亮点

### 1. **端到端的完整流程**
从原始数据到最终预测，每个步骤都有详细的notebook支持

### 2. **严格的可重现性**
- 所有数据来源明确
- 所有参数可追溯
- 完整的错误处理和数据验证

### 3. **多层次验证**
- 交叉验证（训练集内）
- 独立验证（未见过的化合物）
- 基准测试（与其他方法对比）

### 4. **生物学合理性**
- 考虑剂量效应
- 考虑Cmax（药代动力学）
- 通路级特征有生物学意义

### 5. **实用性强**
- 提供预训练模型
- 提供教程notebook
- 可以直接用于新化合物的预测

---

## 📈 数据流转示例

**以一个新化合物为例**：

```
1. 实验数据收集
   └─> 原始RNA-seq计数矩阵（化合物处理 vs DMSO对照）

2. 数据预处理（2.1）
   └─> QC过滤 → DESeq2分析
   └─> 输出：每个基因的log₂FC和FDR

3. 通路特征提取（2.2）
   └─> WikiPathways富集分析
   └─> 输出：每个通路的激活分数（-log₁₀ p值）

4. 模型预测（2.3）
   └─> ToxPredictor模型
   └─> 输出：DILI风险概率

5. 安全边际计算
   └─> 需要IC10和Cmax值
   └─> MOS = IC10 / Cmax
   └─> 如果 MOS < 80，标记为有DILI风险
```

---

## 🔑 核心创新点

1. **转录组学 + 机器学习**：首次系统性地将转录组数据用于DILI预测
2. **剂量感知**：考虑剂量与Cmax的关系，更符合实际应用
3. **通路级特征**：比基因级更稳定，比分子描述符更有生物学意义
4. **安全边际**：提供可解释的风险评估指标
5. **全面基准测试**：与多种方法对比，证明方法的有效性

---

## 📝 总结

**DILImap项目的核心思想**：

> 通过分析化合物处理后的转录组变化，识别受影响的生物学通路，使用机器学习模型将这些通路激活模式转化为DILI风险预测，并考虑剂量和药代动力学因素，提供可解释的安全评估。

**整体设计特点**：
- ✅ **多层次**：基因 → 通路 → 模型
- ✅ **多维度**：转录组 + 药代动力学 + 体外实验
- ✅ **严格验证**：交叉验证 + 独立验证 + 基准测试
- ✅ **实用导向**：提供预训练模型和完整教程

这是一个**从数据到模型到应用**的完整、严谨、可重现的科学研究项目。

