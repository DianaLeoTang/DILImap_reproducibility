# DILImap 项目问题汇总 - 课题组讨论

**整理日期**: 2025年11月20日  
**讨论日期**: 待定（过两天）  
**项目**: DILImap Reproducibility

---

## 📋 目录

1. [核心问题：专有数据访问](#1-核心问题专有数据访问)
2. [受影响的 Notebook](#2-受影响的-notebook)
3. [错误信息详情](#3-错误信息详情)
4. [解决方案](#4-解决方案)
5. [联系信息](#5-联系信息)
6. [其他注意事项](#6-其他注意事项)

---

## 1. 核心问题：专有数据访问

### 问题描述

在运行 DILImap 训练相关的 notebook 时，无法从 S3 访问专有训练数据，导致以下错误：

```
RuntimeError: 无法加载训练数据。请申请数据访问权限或提供本地数据文件。
联系邮箱: DILImap@cellarity.com
```

### 根本原因

- **专有数据限制**：原始训练数据文件受知识产权限制保护
- **需要数据共享协议**：只有在签署数据共享协议并符合机构政策后才能访问
- **S3 访问权限不足**：当前账户没有访问 `s3://dilimap/proprietary/data` 的权限

### 错误类型

- **ClientError**: `NoSuchVersion` - 指定的版本不存在
- **RuntimeError**: 无法加载训练数据

---

## 2. 受影响的 Notebook

### 2.1 需要专有数据的 Notebook（无法运行）

#### ✅ `2.1_Training_Gene_Signatures.ipynb`
- **需要的数据**: `training_data_counts.h5ad`
- **数据位置**: `s3://dilimap/proprietary/data`
- **用途**: 基因表达数据的质量控制 and DESeq2 差异表达分析
- **状态**: ❌ **无法运行** - 需要专有数据访问权限

#### ✅ `2.2_Training_Pathway_Signatures.ipynb`
- **需要的数据**: `training_data_deseq2.h5ad`
- **数据位置**: `s3://dilimap/proprietary/data`
- **用途**: 通路富集分析（WikiPathways）
- **状态**: ❌ **无法运行** - 需要专有数据访问权限
- **依赖**: 需要先运行 `2.1_Training_Gene_Signatures.ipynb` 生成 `training_data_deseq2.h5ad`

### 2.2 可以使用公开数据的 Notebook（可以运行）

#### ✅ `2.3_Training_ToxPredictor_Model.ipynb`
- **需要的数据**: `training_data_pathways.h5ad`
- **数据位置**: `s3://dilimap/public/data` ✅ **公开数据**
- **用途**: 训练 ToxPredictor 模型
- **状态**: ✅ **可以运行** - 使用公开数据

### 2.3 其他 Notebook 状态

- **数据准备阶段** (`1.1`, `1.2`, `1.3`): 使用公开数据，可以运行
- **验证阶段** (`3.1`, `3.2`, `3.3`): 使用公开数据，可以运行
- **结果分析** (`4.1`, `4.2`, `4.3`): 使用公开数据，可以运行

---

## 3. 错误信息详情

### 3.1 完整错误堆栈

```
ClientError: An error occurred (NoSuchVersion) when calling the GetObject operation: 
The specified version does not exist.

Package: s3://dilimap/proprietary/data. Top hash: ede9d7c164
```

### 3.2 错误发生位置

**Notebook**: `2.1_Training_Gene_Signatures.ipynb`  
**Cell**: 数据加载单元格  
**代码**:
```python
adata = dmap.s3.read('training_data_counts.h5ad', package_name='proprietary/data')
```

**Notebook**: `2.2_Training_Pathway_Signatures.ipynb`  
**Cell**: 数据加载单元格  
**代码**:
```python
adata_deseq = dmap.s3.read('training_data_deseq2.h5ad', package_name='proprietary/data')
```

### 3.3 AWS 凭证错误

**错误信息**：
```
AWS credentials not found in environment or .env file.
That didn't work. Unable to locate credentials. Try again.
```

**发生位置**：
- `4.1_Results_Main_Figures.ipynb` - Cell 5: `dmap.s3.login()`
- 其他使用 S3 的 notebook

**原因**：
- AWS 凭证未配置
- 尝试访问 S3 时需要凭证（即使是公开数据，某些操作也可能需要）

**解决方案**：
1. **对于公开数据**：通常不需要登录，代码会自动跳过登录错误
2. **如果需要访问专有数据**：需要配置 AWS 凭证
   - 方法 1：环境变量
     ```bash
     export AWS_ACCESS_KEY_ID=your_access_key
     export AWS_SECRET_ACCESS_KEY=your_secret_key
     ```
   - 方法 2：AWS CLI
     ```bash
     aws configure
     ```
   - 方法 3：创建凭证文件 `~/.aws/credentials`
     ```ini
     [default]
     aws_access_key_id = your_access_key
     aws_secret_access_key = your_secret_key
     ```

**注意**：`4.1_Results_Main_Figures.ipynb` 已更新，会自动处理凭证错误并继续运行。

---

## 4. 解决方案

### 方案 1: 申请数据访问权限（推荐）⭐

**适用场景**: 需要完整重现论文训练过程

**步骤**:
1. **联系项目维护者**
   - 邮箱: **DILImap@cellarity.com**
   - GitHub Issues: [Cellarity/DILImap_reproducibility](https://github.com/Cellarity/DILImap_reproducibility/issues)

2. **申请内容应包括**:
   - 研究目的和用途
   - 机构信息
   - 数据使用计划
   - 数据共享协议签署意愿

3. **等待回复和协议签署**

4. **配置 S3 访问凭证**（获得权限后）

**优点**: 
- 可以完整运行所有训练 notebook
- 获得原始训练数据
- 可以重现完整的研究流程

**缺点**: 
- 需要等待审批
- 需要签署数据共享协议

---

### 方案 2: 使用本地数据文件（如果有）

**适用场景**: 已通过其他渠道获得数据文件

**步骤**:
1. 将数据文件放在以下任一位置：
   - `training_data_counts.h5ad` (当前目录)
   - `../data/training_data_counts.h5ad`
   - `./data/training_data_counts.h5ad`

2. 代码会自动检测并加载本地文件

**优点**: 
- 可以立即运行
- 不需要网络连接

**缺点**: 
- 需要先获得数据文件
- 仍然需要数据访问权限

---

### 方案 3: 跳过训练步骤，使用预训练模型

**适用场景**: 只需要使用模型进行预测，不需要重新训练

**步骤**:
1. **跳过训练阶段** (`2.1`, `2.2`, `2.3`)
2. **直接使用验证和结果分析阶段** (`3.x`, `4.x`)
3. **使用预训练模型**进行预测

**可运行的 Notebook**:
- ✅ `3.1_Validation_Gene_Signatures.ipynb`
- ✅ `3.2_Validation_Pathway_Signatures.ipynb`
- ✅ `3.3_Validation_ToxPredictor_Model.ipynb`
- ✅ `4.1_Results_Main_Figures.ipynb`
- ✅ `4.2_Benchmarking_Insilico_Models.ipynb`
- ✅ `4.3_Benchmarking_Invitro_Models.ipynb`

**优点**: 
- 可以立即使用模型
- 不需要训练数据
- 可以验证模型性能

**缺点**: 
- 无法重新训练模型
- 无法修改训练参数

---

### 方案 4: 使用公开数据训练 ToxPredictor（部分功能）

**适用场景**: 只需要训练 ToxPredictor 模型

**步骤**:
1. **运行 `2.3_Training_ToxPredictor_Model.ipynb`**
   - 这个 notebook 使用公开数据 `training_data_pathways.h5ad`
   - 可以正常训练模型

**优点**: 
- 可以训练 ToxPredictor 模型
- 使用公开数据，无需特殊权限

**缺点**: 
- 无法训练基因和通路特征模型
- 功能受限

---

## 5. 联系信息

### 主要联系方式

- **邮箱**: DILImap@cellarity.com
- **GitHub Issues**: 
  - [Cellarity/DILImap](https://github.com/Cellarity/DILImap/issues)
  - [Cellarity/DILImap_reproducibility](https://github.com/Cellarity/DILImap_reproducibility/issues)

### 项目资源

- **DILImap 网站**: [dilimap.org](https://dilimap.org/)
- **代码仓库**: [github.com/Cellarity/DILImap](https://github.com/Cellarity/DILImap)
- **可重现性仓库**: [github.com/Cellarity/DILImap_reproducibility](https://github.com/Cellarity/DILImap_reproducibility)

---

## 6. 其他注意事项

### 6.1 数据文件说明

| 数据文件 | 类型 | 位置 | 状态 |
|---------|------|------|------|
| `training_data_counts.h5ad` | 专有 | `s3://dilimap/proprietary/data` | ❌ 需要权限 |
| `training_data_deseq2.h5ad` | 专有 | `s3://dilimap/proprietary/data` | ❌ 需要权限 |
| `training_data_pathways.h5ad` | 公开 | `s3://dilimap/public/data` | ✅ 可访问 |

### 6.2 工作流程依赖关系

```
2.1_Training_Gene_Signatures.ipynb
    ↓ (生成)
training_data_deseq2.h5ad
    ↓ (输入)
2.2_Training_Pathway_Signatures.ipynb
    ↓ (生成)
training_data_pathways.h5ad
    ↓ (输入)
2.3_Training_ToxPredictor_Model.ipynb
```

**注意**: 
- `2.2` 依赖于 `2.1` 的输出
- `2.3` 可以使用公开数据，不依赖 `2.1` 和 `2.2`

### 6.3 已实施的改进

✅ **错误处理增强**:
- 在 `2.1_Training_Gene_Signatures.ipynb` 中添加了详细的错误处理
- 自动尝试从本地路径加载数据
- 提供清晰的错误信息和解决方案

✅ **文档更新**:
- 更新了 `TROUBLESHOOTING.md` 文件
- 添加了专有数据访问错误的详细说明

### 6.4 建议的讨论议题

1. **数据访问策略**
   - 是否需要申请完整的数据访问权限？
   - 是否有其他渠道获得数据？

2. **研究目标**
   - 是否需要完整重现训练过程？
   - 还是只需要使用预训练模型进行预测？

3. **时间安排**
   - 数据访问申请需要多长时间？
   - 是否有时间等待审批？

4. **替代方案**
   - 是否可以先使用公开数据训练 ToxPredictor？
   - 是否可以跳过训练步骤，直接使用验证阶段？

---

## 7. 下一步行动

### 立即行动项

- [ ] **决定数据访问策略**（申请权限 vs 使用替代方案）
- [ ] **准备申请材料**（如果选择申请权限）
- [ ] **联系 DILImap@cellarity.com**（申请数据访问）
- [ ] **测试可运行的 notebook**（验证环境配置）

### 短期行动项（1-2周）

- [ ] **等待数据访问审批**
- [ ] **配置 S3 访问凭证**（获得权限后）
- [ ] **运行完整的训练流程**（获得数据后）

### 长期行动项

- [ ] **完成模型训练和验证**
- [ ] **生成所有结果图表**
- [ ] **文档化研究过程**

---

## 8. 问题记录

### 已解决的问题

✅ **FutureWarning: applymap 已弃用**
- 问题: pandas `applymap` 方法已弃用
- 解决: 已替换为 `map` 方法
- 位置: `2.1_Training_Gene_Signatures.ipynb` (Cell 13, 16)

✅ **PyTorch 2.6+ 兼容性问题**
- 问题: `torch.load` 默认 `weights_only=True` 导致无法加载 torch_geometric 对象
- 解决: 添加了 `weights_only=False` 参数和安全全局变量设置
- 位置: `insilico_benchmarks/run_diligenn.py`

✅ **AWS 凭证错误处理**
- 问题: S3 登录失败导致错误
- 解决: 添加了错误处理，允许跳过登录继续使用公开数据
- 位置: `4.1_Results_Main_Figures.ipynb` (Cell 5)

✅ **变量未定义错误处理**
- 问题: `adata` 和 `adata_wiki` 未定义时缺少检查
- 解决: 添加了变量存在性检查和清晰的错误提示
- 位置: 多个 notebook

### 待解决的问题

❌ **专有数据访问权限**
- 问题: 无法访问 S3 上的专有训练数据
- 状态: 等待申请和审批
- 优先级: 高

❌ **gseapy 版本兼容性问题**
- 问题: `enrichr()` 函数收到意外的 `organism` 参数
- 错误信息: `enrichr() got an unexpected keyword argument 'organism'`
- 位置: `3.2_Validation_Pathway_Signatures.ipynb` (Cell 8)
- 状态: 已添加错误处理和回退方案
- 优先级: 中
- 临时解决方案: 
  1. 尝试从 S3 加载预计算的结果
  2. 或更新/降级 gseapy 版本

---

## 附录：相关文件

- `TROUBLESHOOTING.md` - 故障排除指南
- `README.md` - 项目说明
- `INSTALLATION_FORK.md` - 安装说明
- `reproducibility/2.1_Training_Gene_Signatures.ipynb` - 基因特征训练 notebook
- `reproducibility/2.2_Training_Pathway_Signatures.ipynb` - 通路特征训练 notebook

---

**最后更新**: 2025年11月20日  
**维护者**: 课题组  
**联系方式**: DILImap@cellarity.com

